---
phase: 35-allocation-distribution
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - memora_admin/memora_admin/doctype/memora_voucher_allocation/memora_voucher_allocation.js
autonomous: true

must_haves:
  truths:
    - "Admin sees Fill Cards button on Draft allocations, which prompts for quantity and populates the child table"
    - "Admin sees Submit button on Draft allocations with cards, which triggers the approval workflow"
    - "Admin sees Approve and Reject buttons on Pending Approval allocations"
    - "All form fields and child table become read-only after Draft status"
    - "Buttons show appropriate feedback (freeze messages, success alerts) and reload the form after action"
  artifacts:
    - path: "memora_admin/memora_admin/doctype/memora_voucher_allocation/memora_voucher_allocation.js"
      provides: "Fill Cards, Submit, Approve, Reject custom buttons with freeze UI and success alerts"
      min_lines: 80
  key_links:
    - from: "memora_voucher_allocation.js:Fill Cards button"
      to: "api/allocation.py:fill_cards"
      via: "frappe.call with method path"
      pattern: "memora_admin\\.memora_admin\\.api\\.allocation\\.fill_cards"
    - from: "memora_voucher_allocation.js:Submit button"
      to: "api/allocation.py:submit_allocation"
      via: "frappe.call with method path"
      pattern: "memora_admin\\.memora_admin\\.api\\.allocation\\.submit_allocation"
    - from: "memora_voucher_allocation.js:Approve button"
      to: "api/allocation.py:approve_allocation"
      via: "frappe.call with method path"
      pattern: "memora_admin\\.memora_admin\\.api\\.allocation\\.approve_allocation"
---

<objective>
Add interactive form buttons to the Voucher Allocation form for Fill Cards, Submit, Approve, and Reject actions.

Purpose: The API methods from Plan 35-01 need a UI surface. Admin must be able to trigger allocation workflows from the form with appropriate prompts, confirmation dialogs, freeze feedback, and success alerts.

Output: Enhanced `memora_voucher_allocation.js` with conditional buttons based on allocation status.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-allocation-distribution/35-RESEARCH.md
@.planning/phases/35-allocation-distribution/35-01-SUMMARY.md

Key reference files:
@memora_admin/memora_admin/doctype/memora_voucher_batch/memora_voucher_batch.js (established button patterns: Generate Cards with confirm, Void Batch with prompt, Export for Print)
@memora_admin/memora_admin/doctype/memora_voucher_allocation/memora_voucher_allocation.js (existing JS with read-only logic)
@memora_admin/memora_admin/doctype/memora_voucher_allocation/memora_voucher_allocation.json (status field options, allocation_type field)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Fill Cards and Submit buttons to allocation form</name>
  <files>memora_admin/memora_admin/doctype/memora_voucher_allocation/memora_voucher_allocation.js</files>
  <action>
Replace the entire content of `memora_voucher_allocation.js` with the enhanced version. Keep the existing read-only logic and add conditional buttons.

**Structure the `refresh` handler with these button groups:**

**1. Fill Cards button (Draft status, saved form only):**
- Condition: `frm.doc.status === "Draft" && !frm.is_new()`
- Show a `frappe.prompt` dialog asking for quantity:
  ```javascript
  frappe.prompt(
      [{
          fieldname: "quantity",
          fieldtype: "Int",
          label: __("Number of Cards"),
          reqd: 1,
          description: __("Enter the number of cards to fill. Leave 0 to fill all available."),
          default: 0,
      }],
      function (values) {
          frappe.call({
              method: "memora_admin.memora_admin.api.allocation.fill_cards",
              args: {
                  allocation_name: frm.doc.name,
                  quantity: values.quantity || 0,
              },
              freeze: true,
              freeze_message: __("Filling cards..."),
              callback: function (r) {
                  if (r.message) {
                      frappe.show_alert({
                          message: __("{0} cards filled.", [r.message.filled_count]),
                          indicator: "green",
                      });
                      frm.reload_doc();
                  }
              },
          });
      },
      __("Fill Cards"),
      __("Fill")
  );
  ```
- Place in "Actions" group: `frm.add_custom_button(__("Fill Cards"), ..., __("Actions"))`

**2. Submit button (Draft status, saved form, has cards):**
- Condition: `frm.doc.status === "Draft" && !frm.is_new() && frm.doc.allocation_cards && frm.doc.allocation_cards.length > 0`
- Show a `frappe.confirm` dialog: "Submit allocation of {quantity} cards to {customer}? This will start the approval process."
- On confirm, call `memora_admin.memora_admin.api.allocation.submit_allocation`
- Args: `{ allocation_name: frm.doc.name }`
- Freeze: true, freeze_message: "Submitting allocation..."
- Callback: show alert with status (green for "Completed", blue for "Pending Approval"), reload form
- Place in "Actions" group
- Make it primary: `frm.change_custom_button_type(__("Submit Allocation"), __("Actions"), "primary")`

**3. Keep existing read-only logic** for non-Draft statuses (allocation_type, batch, customer, sale_model, allocation_cards fields all read-only).

Follow the established patterns from `memora_voucher_batch.js`:
- Use `frappe.call` with `method`, `args`, `freeze`, `freeze_message`, `callback`
- Use `frappe.show_alert` for success feedback with indicator colors
- Use `frm.reload_doc()` in callbacks to refresh the form
- Use `__()` for all user-facing strings (i18n)
  </action>
  <verify>
Read the file and verify:
1. Fill Cards button appears only on Draft + saved forms
2. Fill Cards uses frappe.prompt for quantity input
3. Submit button appears only on Draft + saved + has cards
4. Submit uses frappe.confirm before calling the API
5. Both buttons are in the "Actions" group
6. Existing read-only logic is preserved for non-Draft statuses
  </verify>
  <done>
Fill Cards button prompts for quantity and calls fill_cards API. Submit Allocation button confirms and calls submit_allocation API. Both buttons appear only on saved Draft allocations. Submit additionally requires cards in the child table.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Approve and Reject buttons for Pending Approval allocations</name>
  <files>memora_admin/memora_admin/doctype/memora_voucher_allocation/memora_voucher_allocation.js</files>
  <action>
Add two more button blocks to the same `refresh` handler, after the Submit button block:

**4. Approve button (Pending Approval status):**
- Condition: `frm.doc.status === "Pending Approval"`
- Show `frappe.confirm`: "Approve this allocation? {quantity} cards will be allocated to {customer}."
- On confirm, call `memora_admin.memora_admin.api.allocation.approve_allocation`
- Args: `{ allocation_name: frm.doc.name }`
- Freeze: true, freeze_message: "Approving allocation..."
- Callback: show green alert "Allocation approved and completed.", reload form
- Place in "Actions" group
- Make it primary: `frm.change_custom_button_type(__("Approve"), __("Actions"), "primary")`

**5. Reject button (Pending Approval status):**
- Condition: `frm.doc.status === "Pending Approval"`
- Show `frappe.prompt` with a Small Text field for reject reason (optional, not required)
- On submit, call `memora_admin.memora_admin.api.allocation.reject_allocation`
- Args: `{ allocation_name: frm.doc.name, reject_reason: values.reject_reason || "" }`
- Freeze: true, freeze_message: "Rejecting allocation..."
- Callback: show orange alert "Allocation rejected.", reload form
- Place in "Actions" group
- Make it danger: `frm.change_custom_button_type(__("Reject"), __("Actions"), "danger")`

**Button visibility summary by status:**

| Status | Buttons Shown |
|--------|--------------|
| Draft (new) | None |
| Draft (saved) | Fill Cards |
| Draft (saved, has cards) | Fill Cards, Submit Allocation |
| Pending Approval | Approve, Reject |
| Approved | None (transient state) |
| Completed | None |
| Rejected | None |
| Cancelled | None |

Ensure the entire JS file is clean, well-formatted with tabs (per ruff/prettier config), and follows the exact patterns from `memora_voucher_batch.js`.
  </action>
  <verify>
Read the file and verify:
1. Approve button appears only on Pending Approval status
2. Reject button appears only on Pending Approval status
3. Approve uses frappe.confirm before calling approve_allocation
4. Reject uses frappe.prompt for optional reject_reason
5. Approve is styled primary, Reject is styled danger
6. All buttons use freeze UI and show_alert on success
7. File uses tabs for indentation (not spaces)
  </verify>
  <done>
Approve button confirms and calls approve_allocation API (primary style). Reject button prompts for reason and calls reject_allocation API (danger style). Both appear only on Pending Approval allocations. Complete button visibility matrix matches the specification.
  </done>
</task>

</tasks>

<verification>
1. Read `memora_voucher_allocation.js` and verify all 4 buttons exist with correct conditions
2. Verify Fill Cards uses `frappe.prompt` with quantity field
3. Verify Submit uses `frappe.confirm` with card count in message
4. Verify Approve uses `frappe.confirm` with card count in message
5. Verify Reject uses `frappe.prompt` with optional reason field
6. Verify all 4 buttons call the correct API methods from `api/allocation.py`
7. Verify existing read-only logic is preserved for non-Draft statuses
8. Verify button type styling: Submit=primary, Approve=primary, Reject=danger
9. Verify file uses tabs for indentation
</verification>

<success_criteria>
- Draft allocations show Fill Cards button (prompts for quantity)
- Draft allocations with cards also show Submit Allocation button (confirms before submitting)
- Pending Approval allocations show Approve (primary) and Reject (danger) buttons
- All buttons use freeze UI, success alerts, and form reload
- Non-Draft statuses make form fields read-only
- All API method paths are correct: memora_admin.memora_admin.api.allocation.*
</success_criteria>

<output>
After completion, create `.planning/phases/35-allocation-distribution/35-02-SUMMARY.md`
</output>
