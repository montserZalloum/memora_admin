---
phase: 01-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fastapi_app/main.py
  - fastapi_app/core/__init__.py
  - fastapi_app/core/config.py
  - fastapi_app/core/logging.py
  - fastapi_app/api/__init__.py
  - fastapi_app/api/deps.py
  - fastapi_app/api/v1/__init__.py
  - fastapi_app/api/v1/router.py
  - fastapi_app/api/v1/endpoints/__init__.py
  - fastapi_app/api/v1/endpoints/health.py
  - .env.example
  - requirements.txt
autonomous: true

must_haves:
  truths:
    - "FastAPI server starts without errors when .env is configured"
    - "GET /api/v1/health/live returns 200 with status 'alive'"
    - "Configuration loads from .env file without hardcoded values"
  artifacts:
    - path: "fastapi_app/main.py"
      provides: "FastAPI app with lifespan context manager"
      contains: "lifespan"
    - path: "fastapi_app/core/config.py"
      provides: "Settings class with pydantic-settings"
      exports: ["Settings", "get_settings"]
    - path: "fastapi_app/core/logging.py"
      provides: "Structured logging configuration"
      exports: ["configure_logging"]
    - path: "fastapi_app/api/v1/endpoints/health.py"
      provides: "Health check endpoints"
      contains: "/live"
  key_links:
    - from: "fastapi_app/main.py"
      to: "fastapi_app/core/config.py"
      via: "get_settings import"
      pattern: "from.*core\\.config.*import.*get_settings"
    - from: "fastapi_app/main.py"
      to: "fastapi_app/api/v1/router.py"
      via: "router include"
      pattern: "app\\.include_router"
---

<objective>
Create FastAPI project scaffold with lifespan events, pydantic-settings configuration, and structured logging.

Purpose: Establish the foundation for the high-performance game API sidecar that will run alongside Frappe, enabling sub-20ms response times for game mechanics.

Output:
- FastAPI application with proper project structure
- Configuration via pydantic-settings loading from .env
- Structured logging (JSON in production, colored console in dev)
- Liveness health check endpoint
- Request ID middleware for correlation
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FastAPI project structure with configuration</name>
  <files>
    fastapi_app/main.py
    fastapi_app/core/__init__.py
    fastapi_app/core/config.py
    fastapi_app/core/logging.py
    .env.example
    requirements.txt
  </files>
  <action>
Create the FastAPI project structure in `fastapi_app/` directory at the root of memora_admin:

**requirements.txt:**
```
fastapi>=0.115.0
uvicorn[standard]>=0.27.0
redis>=5.0.0
pydantic-settings>=2.0.0
structlog>=24.0.0
tenacity>=9.0.0
python-dotenv>=1.0.0
```

**fastapi_app/core/config.py:**
- Create Settings class using `pydantic_settings.BaseSettings`
- Use `SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")`
- Include fields:
  - `redis_url: str = "redis://localhost:6379/0"`
  - `redis_key_prefix: str = "memora:"`
  - `jwt_secret: str` (required, no default)
  - `jwt_algorithm: str = "HS256"`
  - `bitmap_json_path: str` (required, no default)
  - `environment: str = "development"`
  - `api_version: str = "v1"`
  - `slow_redis_threshold_ms: int = 50`
- Create `@lru_cache` decorated `get_settings()` function

**fastapi_app/core/logging.py:**
- Create `configure_logging(environment: str)` function
- Use structlog with shared processors: `add_log_level`, `TimeStamper(fmt="iso")`, `merge_contextvars`
- In production: use `JSONRenderer()`
- In development: use `ConsoleRenderer(colors=True)`
- Call `structlog.configure()` with processors and `stdlib.LoggerFactory()`

**fastapi_app/main.py:**
- Create `@asynccontextmanager async def lifespan(app: FastAPI)` that:
  - Calls `configure_logging(settings.environment)`
  - Logs "FastAPI starting up"
  - `yield`
  - Logs "FastAPI shutting down"
- Create `app = FastAPI(title="Memora Game API", version="1.0.0", lifespan=lifespan)`
- Include router from api.v1

**.env.example:**
```
# Redis Configuration
REDIS_URL=redis://localhost:6379/0
REDIS_KEY_PREFIX=memora:

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production
JWT_ALGORITHM=HS256

# Paths
BITMAP_JSON_PATH=/home/corex/aurevia-bench/sites/x.conanacademy.com/private/memora_bitmaps

# Environment
ENVIRONMENT=development
API_VERSION=v1

# Logging
SLOW_REDIS_THRESHOLD_MS=50
```

Create `fastapi_app/core/__init__.py` as empty file.
  </action>
  <verify>
    - `python -c "from fastapi_app.core.config import Settings, get_settings; print(get_settings())"` runs without import errors
    - `python -c "from fastapi_app.core.logging import configure_logging; configure_logging('development')"` runs without errors
  </verify>
  <done>
    - Settings class loads from .env file
    - Logging configured based on environment
    - All core module imports work
  </done>
</task>

<task type="auto">
  <name>Task 2: Create API router structure with health endpoints and middleware</name>
  <files>
    fastapi_app/api/__init__.py
    fastapi_app/api/deps.py
    fastapi_app/api/v1/__init__.py
    fastapi_app/api/v1/router.py
    fastapi_app/api/v1/endpoints/__init__.py
    fastapi_app/api/v1/endpoints/health.py
    fastapi_app/middleware/__init__.py
    fastapi_app/middleware/request_id.py
  </files>
  <action>
Create the API structure with health endpoints:

**fastapi_app/api/v1/endpoints/health.py:**
```python
from fastapi import APIRouter
from fastapi.responses import JSONResponse
from fastapi_app.core.config import get_settings

router = APIRouter(prefix="/health", tags=["health"])

@router.get("/live")
async def liveness():
    """Liveness check - fast, no dependencies."""
    settings = get_settings()
    return {"status": "alive", "api_version": settings.api_version}
```

**fastapi_app/api/v1/router.py:**
- Create `router = APIRouter(prefix="/api/v1")`
- Include health router

**fastapi_app/middleware/request_id.py:**
```python
import uuid
from starlette.middleware.base import BaseHTTPMiddleware
import structlog

class RequestIDMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request, call_next):
        request_id = str(uuid.uuid4())[:8]
        structlog.contextvars.clear_contextvars()
        structlog.contextvars.bind_contextvars(request_id=request_id)

        response = await call_next(request)
        response.headers["X-Request-ID"] = request_id
        return response
```

**Update fastapi_app/main.py:**
- Add `from fastapi_app.middleware.request_id import RequestIDMiddleware`
- Add `app.add_middleware(RequestIDMiddleware)` after app creation
- Include v1 router: `from fastapi_app.api.v1.router import router as v1_router` and `app.include_router(v1_router)`

Create empty `__init__.py` files in:
- fastapi_app/api/
- fastapi_app/api/v1/
- fastapi_app/api/v1/endpoints/
- fastapi_app/middleware/
  </action>
  <verify>
    - `cd /home/corex/aurevia-bench/apps/memora_admin && cp .env.example .env && uvicorn fastapi_app.main:app --host 0.0.0.0 --port 8001 &` starts server
    - `curl http://localhost:8001/api/v1/health/live` returns `{"status":"alive","api_version":"v1"}`
    - Response includes `X-Request-ID` header
  </verify>
  <done>
    - FastAPI server starts with lifespan events
    - GET /api/v1/health/live returns 200 with status and api_version
    - Request ID middleware adds correlation header
  </done>
</task>

</tasks>

<verification>
1. `pip install -r requirements.txt` completes without errors
2. `cp .env.example .env` creates working configuration
3. `uvicorn fastapi_app.main:app --port 8001` starts without errors
4. `curl http://localhost:8001/api/v1/health/live` returns `{"status":"alive","api_version":"v1"}`
5. Logs appear in colored console format (development mode)
6. Response headers include `X-Request-ID`
</verification>

<success_criteria>
- FastAPI server starts with lifespan events and responds to health check at /api/v1/health/live
- Configuration loads from .env file without hardcoded values
- Structured logging outputs to console in development mode
- Request correlation via X-Request-ID header works
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md`
</output>
