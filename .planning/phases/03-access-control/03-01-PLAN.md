---
phase: 03-access-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fastapi_app/models/access.py
  - fastapi_app/services/season.py
  - memora_admin/memora_admin/events/__init__.py
  - memora_admin/memora_admin/events/access_sync.py
  - memora_admin/memora_admin/hooks.py
autonomous: true

must_haves:
  truths:
    - "Season metadata is cached in Redis hash on doc save"
    - "Gate 1 can check season status without database lookup"
    - "Season cache updates within 1 second of Frappe doc save"
  artifacts:
    - path: "fastapi_app/models/access.py"
      provides: "SeasonMeta Pydantic model"
      exports: ["SeasonMeta"]
    - path: "fastapi_app/services/season.py"
      provides: "SeasonService for Redis hash operations"
      exports: ["SeasonService"]
    - path: "memora_admin/memora_admin/events/access_sync.py"
      provides: "Frappe doc_events handlers for season sync"
      contains: "on_season_updated"
  key_links:
    - from: "memora_admin/memora_admin/events/access_sync.py"
      to: "frappe.cache"
      via: "hset for season metadata"
      pattern: "cache\\.hset"
    - from: "memora_admin/memora_admin/hooks.py"
      to: "memora_admin.events.access_sync"
      via: "doc_events configuration"
      pattern: "Memora Season.*on_update"
---

<objective>
Create season metadata caching infrastructure for Gate 1 validation.

Purpose: Gate 1 needs O(1) season status checks (active + not expired). Frappe doc_events sync season metadata to Redis hash on save, enabling FastAPI to validate season without database lookup.

Output: SeasonMeta model, SeasonService, Frappe doc_events hooks for Memora Season
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-access-control/03-CONTEXT.md
@.planning/phases/03-access-control/03-RESEARCH.md

# Existing code references
@fastapi_app/api/deps.py
@fastapi_app/models/auth.py
@fastapi_app/services/session.py
@memora_admin/memora_admin/hooks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create access models and SeasonService</name>
  <files>
    fastapi_app/models/access.py
    fastapi_app/services/season.py
    fastapi_app/models/__init__.py
    fastapi_app/services/__init__.py
  </files>
  <action>
Create fastapi_app/models/access.py with:
- SeasonMeta Pydantic model:
  - season_id: str
  - is_published: bool
  - start_date: date
  - end_date: date
  - @property is_expired -> bool (date.today() > end_date)
  - @property is_started -> bool (date.today() >= start_date)
  - @property is_active -> bool (is_published and is_started and not is_expired)

Create fastapi_app/services/season.py with SeasonService class:
- __init__(self, redis: redis.Redis, key_prefix: str = "memora:")
- _season_key(season_id) -> f"{prefix}season:{season_id}"
- async get_season_meta(season_id) -> SeasonMeta | None
  - Uses HGETALL to fetch hash
  - Returns None if not cached
  - Parses is_published as "1"/"0" string
  - Parses dates from ISO format strings
- async set_season_meta(season: SeasonMeta) -> None
  - Uses HSET with mapping
  - Stores is_published as "1"/"0"
  - Stores dates as ISO format strings
- async delete_season_meta(season_id) -> None
  - Uses DELETE to remove hash

Update __init__.py files to export new modules.

Use redis.asyncio for async operations matching existing pattern in session.py.
  </action>
  <verify>
- python -c "from fastapi_app.models.access import SeasonMeta; print('SeasonMeta imported')"
- python -c "from fastapi_app.services.season import SeasonService; print('SeasonService imported')"
  </verify>
  <done>
- SeasonMeta model exists with is_expired, is_started, is_active properties
- SeasonService provides async get/set/delete operations on Redis hash
- Models and services import correctly
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Frappe doc_events hooks for season sync</name>
  <files>
    memora_admin/memora_admin/events/__init__.py
    memora_admin/memora_admin/events/access_sync.py
    memora_admin/memora_admin/hooks.py
  </files>
  <action>
Create memora_admin/memora_admin/events/ directory if not exists.

Create memora_admin/memora_admin/events/__init__.py (empty or with docstring).

Create memora_admin/memora_admin/events/access_sync.py with:
- import frappe
- on_season_updated(doc, method):
  - Gets frappe.cache
  - Key: f"memora:season:{doc.name}"
  - Uses cache.hset(key, mapping={...}) for atomic update
  - Stores: is_published (as "1"/"0"), start_date, end_date as ISO strings
  - Logs sync with frappe.logger()
- on_season_deleted(doc, method):
  - Gets frappe.cache
  - Deletes key using cache.delete_value(key)
  - Logs deletion

Update memora_admin/memora_admin/hooks.py:
- Add doc_events dict if not exists
- Add "Memora Season" entry with:
  - "on_update": "memora_admin.events.access_sync.on_season_updated"
  - "after_insert": "memora_admin.events.access_sync.on_season_updated"
  - "on_trash": "memora_admin.events.access_sync.on_season_deleted"

Use frappe.cache (not frappe.cache()) per RESEARCH.md state-of-art section.
  </action>
  <verify>
- ls -la memora_admin/memora_admin/events/
- grep -q "Memora Season" memora_admin/memora_admin/hooks.py && echo "hooks configured"
- python -c "import sys; sys.path.insert(0, 'memora_admin'); from memora_admin.events.access_sync import on_season_updated; print('handler imported')"
  </verify>
  <done>
- events directory created with access_sync.py
- on_season_updated syncs season metadata to Redis hash
- on_season_deleted removes season from Redis
- hooks.py configured with Memora Season doc_events
  </done>
</task>

</tasks>

<verification>
1. SeasonMeta model validates correctly:
   ```python
   from fastapi_app.models.access import SeasonMeta
   from datetime import date
   s = SeasonMeta(season_id="S1", is_published=True, start_date=date(2026,1,1), end_date=date(2026,12,31))
   assert s.is_active == True
   assert s.is_expired == False
   ```

2. SeasonService methods exist:
   ```python
   from fastapi_app.services.season import SeasonService
   import inspect
   assert 'get_season_meta' in dir(SeasonService)
   assert 'set_season_meta' in dir(SeasonService)
   ```

3. Frappe hooks configured:
   ```bash
   grep -A5 "Memora Season" memora_admin/memora_admin/hooks.py
   ```
</verification>

<success_criteria>
- ACCESS-01 (partial): Season validation infrastructure ready
- SeasonMeta model with is_active property for Gate 1 checks
- SeasonService can read/write season metadata from Redis hash
- Frappe doc_events trigger Redis sync on Memora Season save
- All imports work without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-access-control/03-01-SUMMARY.md`
</output>
