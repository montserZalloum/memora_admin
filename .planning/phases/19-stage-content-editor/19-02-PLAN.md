---
phase: 19-stage-content-editor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - memora_admin/memora_admin/services/build/generator.py
  - memora_admin/memora_admin/services/build/plan_generator.py
autonomous: true

must_haves:
  truths:
    - "lesson.json stage_id uses Frappe name field (child table row identifier)"
    - "stage_title field not included in lesson.json output"
    - "Build generators produce valid lesson JSON with correct stage_id values"
  artifacts:
    - path: "memora_admin/memora_admin/services/build/generator.py"
      provides: "Lesson stage data with name-based stage_id"
      contains: "stage.name"
    - path: "memora_admin/memora_admin/services/build/plan_generator.py"
      provides: "Lesson stage data with name-based stage_id"
      contains: "stage.name"
  key_links:
    - from: "Memora Lesson Stage child table"
      to: "lesson.json stages array"
      via: "stage.name → stage_id field"
      pattern: "stage_id.*stage.name"
---

<objective>
Update build generators to use Frappe `name` field as stage_id.

Purpose: After renaming `stage_id` field to `stage_title` in Memora Lesson Stage DocType, the build pipeline must use the Frappe-generated `name` field as the unique stage identifier in lesson.json output.

Output: lesson.json files with correct stage_id values using the child table row name.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
**Schema Change (already done by user):**
- Memora Lesson Stage DocType: `stage_id` field renamed to `stage_title`
- `stage_title` is for admin display only
- Frappe auto-generates `name` field for child table rows (format: random hash like `xxxxxxxxxxxx`)

**Impact:**
- `stage.stage_id` no longer exists → build generators reference broken field
- Must use `stage.name` instead (Frappe's built-in unique identifier)

**API Contract (unchanged):**
- lesson.json `stages[].stage_id` key remains the same
- Client sends `stage_id` in completion requests
- Analytics records `stage_id` for interaction logs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update generator.py to use stage.name</name>
  <files>memora_admin/memora_admin/services/build/generator.py</files>
  <action>
In `_generate_lesson_json` function (around line 272), change:

```python
# Before
"stage_id": stage.stage_id,

# After
"stage_id": stage.name,
```

The `stage.name` is Frappe's auto-generated unique identifier for child table rows.
  </action>
  <verify>
Run: `grep -n "stage.name" memora_admin/memora_admin/services/build/generator.py`
Expected: Line ~272 shows `"stage_id": stage.name`

Run: `grep -n "stage.stage_id" memora_admin/memora_admin/services/build/generator.py`
Expected: No matches (broken reference removed)
  </verify>
  <done>
generator.py uses stage.name for stage_id in lesson JSON output
  </done>
</task>

<task type="auto">
  <name>Task 2: Update plan_generator.py to use stage.name</name>
  <files>memora_admin/memora_admin/services/build/plan_generator.py</files>
  <action>
In `_generate_lesson_json` function (around line 525), change:

```python
# Before
"stage_id": stage.stage_id,

# After
"stage_id": stage.name,
```

Same fix as Task 1, ensuring consistency across both generators.
  </action>
  <verify>
Run: `grep -n "stage.name" memora_admin/memora_admin/services/build/plan_generator.py`
Expected: Line ~525 shows `"stage_id": stage.name`

Run: `grep -n "stage.stage_id" memora_admin/memora_admin/services/build/plan_generator.py`
Expected: No matches (broken reference removed)
  </verify>
  <done>
plan_generator.py uses stage.name for stage_id in lesson JSON output
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify no remaining stage_id field references</name>
  <files>N/A (verification only)</files>
  <action>
Search for any remaining `stage.stage_id` references in the codebase that might need updating.

Expected: Only legitimate uses remain (e.g., Pydantic models, other DocTypes with their own stage_id field).
  </action>
  <verify>
Run: `grep -rn "stage\.stage_id" memora_admin/memora_admin/services/`
Expected: No matches

Run: `grep -rn "\.stage_id" memora_admin/memora_admin/services/build/`
Expected: No matches in generator files
  </verify>
  <done>
All build generator stage_id references updated to use stage.name
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Code verification:
   - No `stage.stage_id` references in build generators
   - Both generators use `stage.name` for stage_id

2. JSON output structure (unchanged):
   - lesson.json still has `stages[].stage_id` key
   - Value now comes from `stage.name` (Frappe child row identifier)
   - `stage_title` field NOT included in JSON output (admin-only)

3. API compatibility:
   - Client receives same JSON structure
   - Analytics still records stage_id correctly
</verification>

<success_criteria>
1. generator.py:272 uses `stage.name` instead of `stage.stage_id`
2. plan_generator.py:525 uses `stage.name` instead of `stage.stage_id`
3. No `stage.stage_id` references remain in build services
4. lesson.json output structure unchanged (same keys, different value source)
</success_criteria>

<output>
After completion, create `.planning/phases/19-stage-content-editor/19-02-SUMMARY.md`
</output>
