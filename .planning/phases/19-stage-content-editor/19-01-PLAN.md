---
phase: 19-stage-content-editor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - memora_admin/memora_admin/doctype/memora_lesson_stage/memora_lesson_stage.json
  - memora_admin/public/js/game_lesson.js
autonomous: true

must_haves:
  truths:
    - "Edit Content button appears in Memora Lesson Stage child table rows"
    - "Clicking button opens type-specific dialog for MATCHING, REVEAL, SENTENCE_BUILDER"
    - "Dialog pre-populates with existing config_json data"
    - "Save action stores JSON in config_json field"
    - "Unsupported stage types show informative message"
  artifacts:
    - path: "memora_admin/memora_admin/doctype/memora_lesson_stage/memora_lesson_stage.json"
      provides: "Button field for triggering edit dialogs"
      contains: "edit_content_btn"
    - path: "memora_admin/public/js/game_lesson.js"
      provides: "Dialog handlers for stage content editing"
      contains: "config_json"
  key_links:
    - from: "memora_lesson_stage.json"
      to: "game_lesson.js"
      via: "edit_content_btn field triggers handler"
      pattern: "edit_content_btn"
    - from: "game_lesson.js"
      to: "config_json field"
      via: "frappe.model.set_value saves to correct field"
      pattern: "set_value.*config_json"
---

<objective>
Wire up stage content editor button and fix existing dialog JavaScript bugs.

Purpose: Enable admins to edit lesson stage content via type-specific dialogs directly from the Memora Lesson form's stages child table.

Output: Working "Edit Content" button in stage rows that opens MATCHING, REVEAL, or SENTENCE_BUILDER dialogs, saves to config_json field.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@memora_admin/memora_admin/doctype/memora_lesson_stage/memora_lesson_stage.json
@memora_admin/public/js/game_lesson.js
@memora_admin/hooks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add edit_content_btn button field to Memora Lesson Stage DocType</name>
  <files>memora_admin/memora_admin/doctype/memora_lesson_stage/memora_lesson_stage.json</files>
  <action>
Add a Button field to the Memora Lesson Stage child table schema:

1. Add "edit_content_btn" to field_order array (after config_json)
2. Add field definition to fields array:
   ```json
   {
     "fieldname": "edit_content_btn",
     "fieldtype": "Button",
     "label": "Edit Content",
     "in_list_view": 1
   }
   ```
3. Update modified timestamp to current time

The button must have `in_list_view: 1` to appear in the editable grid rows.
  </action>
  <verify>
Run: `python3 -c "import json; d=json.load(open('memora_admin/memora_admin/doctype/memora_lesson_stage/memora_lesson_stage.json')); print('edit_content_btn' in d['field_order'] and any(f['fieldname']=='edit_content_btn' for f in d['fields']))"`
Expected: True
  </verify>
  <done>
edit_content_btn field exists in schema with fieldtype=Button, in_list_view=1
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix field name bugs in game_lesson.js</name>
  <files>memora_admin/public/js/game_lesson.js</files>
  <action>
Fix the following bugs in the existing game_lesson.js file:

1. Line 11: Change `row.type` to `row.stage_type` (checking wrong field for empty validation)

2. Line 17: Change `row.config` to `row.config_json` (reading from wrong field)

3. Line 162 in open_reveal_dialog: Change `'config'` to `'config_json'` in frappe.model.set_value call

4. Line 232 in open_sentence_builder_dialog: Change `'config'` to `'config_json'` in frappe.model.set_value call

The open_matching_dialog function (line 92) already correctly uses 'config_json' - the other dialogs need to match this.

All three dialog save functions should use:
```javascript
frappe.model.set_value(cdt, cdn, 'config_json', JSON.stringify(config_payload, null, 2));
```
  </action>
  <verify>
Run: `grep -n "row.config\|row.type\|'config'" memora_admin/public/js/game_lesson.js`
Expected: No matches for row.config, row.type (outside stage_type context), or 'config' (should all be 'config_json')

Run: `grep -c "config_json" memora_admin/public/js/game_lesson.js`
Expected: 4 or more occurrences (reading + 3 saves)
  </verify>
  <done>
All field references use correct names: stage_type for validation, config_json for reading and saving
  </done>
</task>

<task type="auto">
  <name>Task 3: Run bench migrate and verify button appears</name>
  <files>N/A (verification only)</files>
  <action>
Run Frappe bench migrate to apply the DocType schema changes:

```bash
cd /home/corex/aurevia-bench && bench migrate
```

Then clear Frappe cache to ensure JS changes take effect:

```bash
bench clear-cache
```

Verify by checking the schema was updated in database (optional, migrate output sufficient).
  </action>
  <verify>
Run bench migrate command - should complete without errors.
Run bench clear-cache - should complete without errors.
  </verify>
  <done>
Schema migrated, cache cleared, edit_content_btn button will appear in Memora Lesson Stage rows on next form load.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Schema verification:
   - `edit_content_btn` field exists in memora_lesson_stage.json
   - Field has `in_list_view: 1` to appear in grid

2. JavaScript verification:
   - No references to `row.config` (should be `row.config_json`)
   - No references to `row.type` (should be `row.stage_type`)
   - All set_value calls use `'config_json'` field name

3. Integration verification:
   - game_lesson.js already loaded via hooks.py doctype_js
   - bench migrate completed successfully
</verification>

<success_criteria>
1. edit_content_btn Button field added to Memora Lesson Stage schema with in_list_view=1
2. game_lesson.js uses correct field names (stage_type, config_json)
3. bench migrate completes without errors
4. All three dialog types (MATCHING, REVEAL, SENTENCE_BUILDER) save to config_json field
</success_criteria>

<output>
After completion, create `.planning/phases/19-stage-content-editor/19-01-SUMMARY.md`
</output>
