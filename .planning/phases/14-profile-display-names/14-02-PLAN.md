---
phase: 14-profile-display-names
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - memora_admin/events/profile_sync.py
  - memora_admin/api/profile.py
  - memora_admin/hooks.py
  - fastapi_app/core/pubsub.py
  - memora_admin/tasks/profile_cache.py
autonomous: true

must_haves:
  truths:
    - "Profile updates in Frappe push to Redis cache immediately"
    - "Pub/sub listener invalidates ProfileService cache on profile message"
    - "Frappe API can batch-fetch profiles for cache miss handling"
    - "Hourly task pre-warms cache for active leaderboard players"
  artifacts:
    - path: "memora_admin/events/profile_sync.py"
      provides: "on_player_profile_updated hook"
      min_lines: 25
      exports: ["on_player_profile_updated"]
    - path: "memora_admin/api/profile.py"
      provides: "get_profiles_batch Frappe API"
      contains: "@frappe.whitelist"
    - path: "memora_admin/tasks/profile_cache.py"
      provides: "warm_profile_cache scheduled task"
      min_lines: 40
    - path: "fastapi_app/core/pubsub.py"
      provides: "profile invalidation handler"
      contains: "profile"
  key_links:
    - from: "memora_admin/events/profile_sync.py"
      to: "frappe.cache"
      via: "SET with TTL on profile update"
      pattern: "cache.*set"
    - from: "memora_admin/hooks.py"
      to: "memora_admin/events/profile_sync.py"
      via: "doc_events registration"
      pattern: "on_player_profile_updated"
    - from: "fastapi_app/core/pubsub.py"
      to: "profile_service.invalidate"
      via: "message handler dispatch"
      pattern: "profile_service.*invalidate"
---

<objective>
Integrate Frappe with ProfileService cache via hooks, API, pub/sub, and scheduled warming.

Purpose: Ensure profile cache stays fresh through push-on-update (sub-second invalidation), pull-on-miss (Frappe batch API), and proactive warming (hourly task for active players).

Output: Frappe hook for cache push, batch profile API, pub/sub invalidation handler, and scheduled pre-warming task.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-profile-display-names/14-CONTEXT.md
@.planning/phases/14-profile-display-names/14-RESEARCH.md

# Existing patterns to follow:
@memora_admin/events/access_sync.py
@memora_admin/events/device_sync.py
@memora_admin/hooks.py
@fastapi_app/core/pubsub.py
@memora_admin/tasks/leaderboard_reset.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create profile_sync hook and batch API</name>
  <files>
    memora_admin/events/profile_sync.py
    memora_admin/api/profile.py
    memora_admin/hooks.py
  </files>
  <action>
Create `memora_admin/events/profile_sync.py`:
- Follow access_sync.py pattern for structure
- Import frappe, json
- `CACHE_TTL = 3600` (1 hour)
- Function `on_player_profile_updated(doc, method)`:
  - Get Redis via `frappe.cache()`
  - Build key: `f"memora:profile:{doc.user}"`
  - Build profile data dict: `{"player_id": doc.user, "display_name": doc.display_name or "", "avatar": doc.avatar or "default_avatar"}`
  - SET with TTL: `cache.set(redis_key, json.dumps(profile_data), ex=CACHE_TTL)`
  - Also publish invalidation message for any FastAPI instances that cached it:
    - `cache.publish("memora:cache:invalidate", json.dumps({"type": "profile", "player_id": doc.user, "timestamp": time.time()}))`
  - Log: `frappe.logger().info(f"Profile {doc.user} synced to Redis")`

Create `memora_admin/api/profile.py`:
- Import frappe
- `@frappe.whitelist(allow_guest=False)`
- Function `get_profiles_batch(player_ids: list[str]) -> list[dict]`:
  - If not player_ids: return []
  - Query: `frappe.get_all("Memora Player Profile", filters={"user": ["in", player_ids]}, fields=["user", "display_name", "avatar"])`
  - Transform to list of dicts: `[{"player_id": p.user, "display_name": p.display_name or "", "avatar": p.avatar or "default_avatar"} for p in profiles]`
  - Return the list

Update `memora_admin/hooks.py`:
- In the `doc_events` dict, modify the existing "Memora Player Profile" entry to ADD the profile sync hook:
  - Current: `"on_update": "memora_admin.events.device_sync.on_player_profile_update"`
  - Change to list for on_update: `"on_update": ["memora_admin.events.device_sync.on_player_profile_update", "memora_admin.events.profile_sync.on_player_profile_updated"]`
  - Also add `"after_insert"` hook for new profiles: `"after_insert": "memora_admin.events.profile_sync.on_player_profile_updated"`
  </action>
  <verify>
`python -c "from memora_admin.events.profile_sync import on_player_profile_updated; from memora_admin.api.profile import get_profiles_batch; print('Import OK')"` succeeds within bench context
  </verify>
  <done>
Profile update hook pushes to Redis cache. Batch API available for FastAPI cache-miss handling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add pub/sub handler and scheduled task</name>
  <files>
    fastapi_app/core/pubsub.py
    memora_admin/tasks/profile_cache.py
    memora_admin/hooks.py
  </files>
  <action>
Update `fastapi_app/core/pubsub.py`:
- In `_handle_invalidation` function, add elif branch for profile messages (following the existing pattern for hierarchy/plan):
```python
elif msg_type == "profile" and payload.get("player_id"):
    player_id = payload.get("player_id")
    profile_service = getattr(app_state, "profile_service", None)

    if profile_service:
        await profile_service.invalidate(player_id)
        logger.info(
            "profile_cache_invalidated",
            player_id=player_id,
            timestamp=timestamp,
        )
    else:
        logger.warning(
            "profile_service_not_available",
            player_id=player_id,
        )
```

Create `memora_admin/tasks/profile_cache.py`:
- Follow leaderboard_reset.py pattern for structure
- Import: frappe, redis, json, logging, datetime
- Import task_utils: AMMAN_TZ, TASK_DURATION, TASK_RUNS, has_run_today, log_task_run, notify_admins (if available, otherwise skip metrics)
- Constants: `CACHE_TTL = 3600`, `LB_PREFIX = "memora:lb"`
- Helper: `get_redis()` returns `redis.from_url(frappe.conf.redis_cache)`
- Main function `warm_profile_cache()`:
  - task_name = "profile_cache_warm"
  - Idempotency check with has_run_today (if task_utils available)
  - Collect unique player_ids from top 100 of active leaderboards:
    - alltime: `r.zrange("memora:lb:alltime", 0, 99, desc=True)`
    - today's daily: `r.zrange(f"memora:lb:daily:{today_str}", 0, 99, desc=True)` where today_str = datetime.now(AMMAN_TZ).strftime("%Y-%m-%d")
    - current weekly: `r.zrange(f"memora:lb:weekly:{week_str}", 0, 99, desc=True)` where week_str = datetime.now(AMMAN_TZ).strftime("%G-W%V")
  - Dedupe player_ids into a set
  - Batch fetch from Frappe: `frappe.get_all("Memora Player Profile", filters={"user": ["in", list(player_ids)]}, fields=["user", "display_name", "avatar"])`
  - Pipeline SET to cache each profile with TTL
  - Log success count
- Wrap in try/except with log_task_run for metrics (following leaderboard_reset pattern)

Update `memora_admin/hooks.py`:
- Add to scheduler_events["cron"]:
```python
# Hourly at :30: Pre-warm profile cache for active leaderboard players
"30 * * * *": [
    "memora_admin.tasks.profile_cache.warm_profile_cache"
],
```
  </action>
  <verify>
1. `grep -n "profile" fastapi_app/core/pubsub.py` shows the new handler
2. `grep -n "profile_cache" memora_admin/hooks.py` shows scheduled task
  </verify>
  <done>
Pub/sub listener handles profile invalidation messages. Hourly task pre-warms cache for active leaderboard players.
  </done>
</task>

</tasks>

<verification>
1. Hook registered in hooks.py for Memora Player Profile on_update and after_insert
2. Pub/sub handler dispatches to profile_service.invalidate()
3. Scheduled task registered at :30 hourly
4. Frappe API get_profiles_batch returns correct format
</verification>

<success_criteria>
- Profile update in Frappe triggers immediate Redis cache push
- Pub/sub invalidation message published on profile update
- FastAPI pub/sub listener handles profile messages
- Frappe batch API returns profiles in expected format
- Hourly scheduled task warms cache for top 100 players from each leaderboard
</success_criteria>

<output>
After completion, create `.planning/phases/14-profile-display-names/14-02-SUMMARY.md`
</output>
