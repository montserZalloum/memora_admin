---
phase: 38-reports-season-expiration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - memora_admin/memora_admin/report/__init__.py
  - memora_admin/memora_admin/report/sales_by_library/__init__.py
  - memora_admin/memora_admin/report/sales_by_library/sales_by_library.json
  - memora_admin/memora_admin/report/sales_by_library/sales_by_library.py
  - memora_admin/memora_admin/report/sales_by_library/sales_by_library.js
  - memora_admin/memora_admin/report/batch_performance/__init__.py
  - memora_admin/memora_admin/report/batch_performance/batch_performance.json
  - memora_admin/memora_admin/report/batch_performance/batch_performance.py
  - memora_admin/memora_admin/report/batch_performance/batch_performance.js
autonomous: true

must_haves:
  truths:
    - "Admin can open Sales by Library report filtered by date range and library, seeing redeemed card count, face value, commission, net revenue, and invoice status per library"
    - "Admin can open Batch Performance report showing card status distribution per batch with redemption rate percentage and days until season end"
    - "Both reports display report_summary indicators at the top and add_total_row for numeric columns"
  artifacts:
    - path: "memora_admin/memora_admin/report/__init__.py"
      provides: "Report module package init"
    - path: "memora_admin/memora_admin/report/sales_by_library/sales_by_library.py"
      provides: "RPT-01 execute() with SQL JOIN, commission calculation, report_summary"
      exports: ["execute"]
    - path: "memora_admin/memora_admin/report/sales_by_library/sales_by_library.json"
      provides: "RPT-01 Script Report metadata for Frappe Desk"
      contains: "Script Report"
    - path: "memora_admin/memora_admin/report/sales_by_library/sales_by_library.js"
      provides: "RPT-01 client-side filters (from_date, to_date, library)"
    - path: "memora_admin/memora_admin/report/batch_performance/batch_performance.py"
      provides: "RPT-02 execute() with card status CASE aggregation, season subquery"
      exports: ["execute"]
    - path: "memora_admin/memora_admin/report/batch_performance/batch_performance.json"
      provides: "RPT-02 Script Report metadata for Frappe Desk"
      contains: "Script Report"
    - path: "memora_admin/memora_admin/report/batch_performance/batch_performance.js"
      provides: "RPT-02 client-side filters (batch, status)"
  key_links:
    - from: "memora_admin/memora_admin/report/sales_by_library/sales_by_library.py"
      to: "memora_admin/memora_admin/services/voucher/commission.py"
      via: "import resolve_commission, calculate_commission"
      pattern: "from memora_admin.*commission import"
    - from: "memora_admin/memora_admin/report/batch_performance/batch_performance.py"
      to: "tabMemora Season"
      via: "subquery JOIN through Batch Grant -> Product Grant -> Plan -> Season"
      pattern: "tabMemora Season"
---

<objective>
Create the report/ module directory and implement two Script Reports: Sales by Library (RPT-01) and Batch Performance (RPT-02).

Purpose: Give admins operational visibility into voucher sales performance per library (with commission/revenue breakdown) and per-batch card status distribution (with redemption rate and season countdown).

Output: 9 files across report/ parent directory + 2 report subdirectories, each with .py/.js/.json/__init__.py following the ERPNext Script Report pattern.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-reports-season-expiration/38-RESEARCH.md
@memora_admin/memora_admin/services/voucher/commission.py
@memora_admin/memora_admin/services/voucher/invoice.py
@memora_admin/hooks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sales by Library Script Report (RPT-01)</name>
  <files>
    memora_admin/memora_admin/report/__init__.py
    memora_admin/memora_admin/report/sales_by_library/__init__.py
    memora_admin/memora_admin/report/sales_by_library/sales_by_library.json
    memora_admin/memora_admin/report/sales_by_library/sales_by_library.py
    memora_admin/memora_admin/report/sales_by_library/sales_by_library.js
  </files>
  <action>
    1. Create `memora_admin/memora_admin/report/__init__.py` (empty file -- required for Python package discovery).

    2. Create `memora_admin/memora_admin/report/sales_by_library/__init__.py` (empty file).

    3. Create `sales_by_library.json` -- Script Report metadata:
       - `"module": "Memora Admin"` (must match modules.txt)
       - `"ref_doctype": "Memora Voucher Card"` (primary DocType)
       - `"report_type": "Script Report"`
       - `"is_standard": "Yes"`
       - `"report_name": "Sales by Library"`
       - `"name": "Sales by Library"`
       - `"add_total_row": 1` (auto-totals for numeric columns)
       - `"roles": [{"role": "System Manager"}]`
       - `"owner"/"modified_by": "Administrator"`

    4. Create `sales_by_library.js` with filters:
       - `from_date`: Date, default = 1 month ago, reqd: 1
       - `to_date`: Date, default = today, reqd: 1
       - `library`: Link to Customer (optional)
       - `sale_model`: Select with options "" (All), "Prepaid", "Consignment" (optional)
       Use `frappe.query_reports["Sales by Library"]` format.

    5. Create `sales_by_library.py` with `execute(filters=None)`:
       - **Columns:** library (Link:Customer, 200w), redeemed_count (Int, 120w), face_value (Currency, 120w), total_face_value (Currency, 140w), commission_per_card (Currency, 140w), total_commission (Currency, 140w), net_revenue (Currency, 140w), sale_model (Data, 120w), invoice_status (Data, 120w)
       - **get_data(filters):**
         - Query `tabMemora Voucher Card c JOIN tabMemora Voucher Batch b ON c.batch = b.name`
         - WHERE: `c.status = 'Redeemed'`
         - Optional filters: `c.redeemed_at >= %s` (from_date), `c.redeemed_at <= %s + " 23:59:59"` (to_date), `c.library = %s` (library), `c.sale_model = %s` (sale_model)
         - GROUP BY: `c.library, c.batch, c.sale_model`
         - SELECT: `c.library, c.batch, b.batch_name, COUNT(*) as redeemed_count, b.face_value, c.sale_model`
         - Also SELECT invoice_status: `CASE WHEN c.sales_invoice IS NOT NULL AND c.sales_invoice != '' THEN 'Invoiced' ELSE 'Not Invoiced' END`
         - Add invoice_status to GROUP BY
       - **Post-processing in Python:** For each row, call `resolve_commission(row.batch, row.library)` and `calculate_commission(face_value=str(row.face_value), quantity=row.redeemed_count, commission_type=ct, commission_value=cv)` to get commission_per_card, total_commission, net_revenue. Use `float()` to convert Decimal results for Frappe column rendering.
       - **get_report_summary(data):** Show total redeemed count (Green indicator), total net revenue (Blue indicator, Currency with "JOD"), total commission (Grey indicator, Currency with "JOD").
       - **Return:** `columns, data, None, None, report_summary` (no chart needed).
       - Import from `memora_admin.memora_admin.services.voucher.commission` for `resolve_commission` and `calculate_commission`. Do NOT re-implement commission logic.
       - Use `filters.get("fieldname")` pattern (never raw attribute access) to avoid crashes on missing optional filters.
       - Use `from frappe import _` for translatable column labels.
  </action>
  <verify>
    Run `bench --site x.conanacademy.com execute memora_admin.memora_admin.report.sales_by_library.sales_by_library.execute` to confirm no import errors and the function returns (columns, data, None, None, report_summary) tuple.
  </verify>
  <done>
    Sales by Library report Python module executes without errors, returns proper column definitions with Link/Currency/Int fieldtypes, queries redeemed cards with commission calculation using existing resolve_commission + calculate_commission, and includes report_summary with total redeemed/revenue/commission indicators.
  </done>
</task>

<task type="auto">
  <name>Task 2: Batch Performance Script Report (RPT-02)</name>
  <files>
    memora_admin/memora_admin/report/batch_performance/__init__.py
    memora_admin/memora_admin/report/batch_performance/batch_performance.json
    memora_admin/memora_admin/report/batch_performance/batch_performance.py
    memora_admin/memora_admin/report/batch_performance/batch_performance.js
  </files>
  <action>
    1. Create `batch_performance/__init__.py` (empty file).

    2. Create `batch_performance.json` -- Script Report metadata:
       - `"module": "Memora Admin"`
       - `"ref_doctype": "Memora Voucher Batch"` (primary DocType)
       - `"report_type": "Script Report"`, `"is_standard": "Yes"`
       - `"report_name": "Batch Performance"`, `"name": "Batch Performance"`
       - `"add_total_row": 0` (totals don't make sense for per-batch metrics like redemption_rate or days_until_end)
       - `"roles": [{"role": "System Manager"}]`

    3. Create `batch_performance.js` with filters:
       - `batch`: Link to Memora Voucher Batch (optional)
       - `status`: Select with options "" (All), "Generated", "Active", "Closed" (optional, batch status filter)

    4. Create `batch_performance.py` with `execute(filters=None)`:
       - **Columns:** batch (Link:Memora Voucher Batch, 150w), batch_name (Data, 150w), face_value (Currency, 100w), total_cards (Int, 90w), available (Int, 90w), allocated (Int, 90w), redeemed (Int, 90w), voided (Int, 80w), expired (Int, 80w), redemption_rate (Percent, 110w), season_end (Date, 110w), days_until_end (Int, 110w)
       - **get_data(filters):**
         - Use a subquery for season info to avoid multiple rows per batch when multiple grants link to different seasons. Subquery: `SELECT bg.parent as batch_name, MIN(s.end_date) as end_date FROM tabMemora Voucher Batch Grant bg JOIN tabMemora Product Grant pg ON bg.product_grant = pg.name JOIN tabMemora Academic Plan ap ON pg.plan = ap.name JOIN tabMemora Season s ON ap.season = s.name GROUP BY bg.parent`
         - Main query: `SELECT b.name as batch, b.batch_name, b.face_value, b.quantity as total_cards, SUM(CASE WHEN c.status='Available' THEN 1 ELSE 0 END) as available, SUM(CASE WHEN c.status='Allocated' THEN 1 ELSE 0 END) as allocated, SUM(CASE WHEN c.status='Redeemed' THEN 1 ELSE 0 END) as redeemed, SUM(CASE WHEN c.status='Void' THEN 1 ELSE 0 END) as voided, SUM(CASE WHEN c.status='Expired' THEN 1 ELSE 0 END) as expired, ROUND(SUM(CASE WHEN c.status='Redeemed' THEN 1 ELSE 0 END) * 100.0 / NULLIF(b.quantity, 0), 1) as redemption_rate, season_info.end_date as season_end, DATEDIFF(season_info.end_date, CURDATE()) as days_until_end`
         - `FROM tabMemora Voucher Batch b LEFT JOIN tabMemora Voucher Card c ON c.batch = b.name LEFT JOIN (subquery) season_info ON season_info.batch_name = b.name`
         - WHERE: `b.status != 'Draft'` (exclude drafts)
         - Optional filters: `b.name = %s` (batch), `b.status = %s` (status)
         - GROUP BY: `b.name, b.batch_name, b.face_value, b.quantity, season_info.end_date`
         - ORDER BY: `b.creation DESC`
       - **get_report_summary(data):** Total cards across all batches (Data indicator), total redeemed (Green), average redemption rate (Blue, Percent).
       - **Return:** `columns, data, None, None, report_summary`
       - Handle NULL season_end: if `days_until_end` is None, display as-is (Frappe renders NULL as empty cell, which is acceptable for batches without season linkage).
  </action>
  <verify>
    Run `bench --site x.conanacademy.com execute memora_admin.memora_admin.report.batch_performance.batch_performance.execute` to confirm no import errors and the function returns the expected tuple with column definitions.
  </verify>
  <done>
    Batch Performance report Python module executes without errors, returns card status distribution per batch using CASE/SUM aggregation, computes redemption_rate as percentage via SQL, resolves season_end using the earliest season from the batch grant chain (MIN subquery), and includes report_summary with totals.
  </done>
</task>

</tasks>

<verification>
1. Both report directories exist under `memora_admin/memora_admin/report/` with all 4 required files each (.py, .js, .json, __init__.py)
2. Each report's `execute()` function can be called via `bench execute` without import errors
3. JSON metadata has correct module ("Memora Admin"), report_type ("Script Report"), is_standard ("Yes")
4. Sales by Library uses existing commission service (not re-implemented)
5. Batch Performance uses MIN subquery for season end date (handles multi-grant batches)
</verification>

<success_criteria>
- Sales by Library report returns columns, data, and report_summary with redeemed card counts, face value, commission, net revenue, and invoice status grouped by library/batch/sale_model
- Batch Performance report returns card status distribution per batch with redemption rate percentage and days until season end (using earliest season across grants)
- Both reports follow the ERPNext Script Report 4-file pattern and can be discovered by Frappe Desk
</success_criteria>

<output>
After completion, create `.planning/phases/38-reports-season-expiration/38-01-SUMMARY.md`
</output>
